<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace: 매퍼 인터페이스의 풀패키지 경로 -->
<mapper namespace="com.mjc813.chapter21.gamecrud.GameMybatis">

    <insert id="insertData" parameterType="GameDto">
        INSERT INTO game_tbl (
              name
            , genre
            , grade
            , price
            , img_url
        )
        VALUES (
            #{name}
            , #{genre}
            , #{grade}
            , #{price}
            , #{imgUrl}
        )
    </insert>

    <update id="updateData" parameterType="GameDto">
        UPDATE game_tbl SET
            name = #{name}
            , genre = #{genre}
            , grade = #{grade}
            , price = #{price}
            , img_url = #{imgUrl}
        WHERE id = #{id}
    </update>

    <delete id="deleteData" parameterType="Integer">
        DELETE FROM game_tbl
        WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="Integer" resultType="GameDto">
        SELECT id, name, genre, grade, price, img_url
        FROM game_tbl
        WHERE id = #{id}
    </select>
    
    <select id="getAllGames" parameterType="com.mjc813.chapter21.gamecrud.GameDto">
        SELECT id, name, genre, grade, price, img_url
        FROM game_tbl
        ORDER BY id ASC
    </select>
</mapper>