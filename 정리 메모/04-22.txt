04-22
방어코딩 : setCookieId,getCookieId 중 어느 것이 호출될 지 모르기 때문에 양쪽 다 cookie==null인지 항상 검사해서 null이라면 new Cookie()로 만들고 그 id에 할당한다.
setter,getter에서는 멤버변수를 직접 호출한다.

서비스에서 항상 Dto로 객체를 받고, 리턴또한 Dto로 해야 한다.
받은 후에는 Dto에서 Entity로 형변환 해야하는데, 이유는 Repository가 Entity로 값을 받으므로 Entity로 형변환 해야 한다.

mybatis에서 새 id가 만들어질때 keyProperties, use~를 쓰면 정확히 id를 받아올 수 있다

조인을 여러번 해서 가져오는 방법
1. Entity의 @을 아래와 같이 작성하고
@NamedEntityGraph(name = "RecipeEntity.fetchCookieIngredient"
    , attributeNodes = {
        @NamedAttributeNode(value = "cookie"),
        @NamedAttributeNode(value = "ingredient", subgraph = "RecipeIngredient.fetchCategory")}
    , subgraphs = {
        @NamedSubgraph(name ="RecipeIngredient.fetchCategory", attributeNodes = {
            @NamedAttributeNode(value = "category")})
    }
)
repository에 실행할 crud에 @EntityGraph(value = "RecipeEntity.fetchCookieIngredient")를 붙인다.

2. repository에 실행할 crud에 @EntityGraph(attributePaths = {"cookie", "ingredient", "ingredient.category"})를 붙힌다.
@EntityGraph(attributePaths = {"멤버변수이름1", "멤버변수이름2","외래키멤버변수이름1.외래키멤버변수이름2....) 사용하고있는 멤버변수이름과 정확히 일치해야한다.

방법2가 더 편하다.
스프링부트 yaml파일 db와 user와 password가  각각, cookies, cookies_user, cookies9876 일때
db에서 실행할 문장
CREATE DATABASE cookies DEFAULT CHARACTER SET utf8mb4;
CREATE USER 'cookies_user'@'%' IDENTIFIED BY 'cookies9876';
GRANT ALL PRIVILEGES ON cookies.* TO 'cookies_user'@'%';
FLUSH PRIVILEGES;
-----------------------------
04-23
첨부파일 추가 기능
무조건 @PostMapping으로, 매개변수에는 @RequestPart가 와야함

로그 찍는 방법
@Slf4j 어노테이션 사용후 //로그를 기록하는 어노테이션
log.info(String, 데이터);
-----------------------------
04-24
로그 파일 설정한 장소로 로그가 출력된다(파일, 화면...) 기본은 콘솔
로그 파일 설정 : yml파일로
각 로그 출력에 대한 레벨
log.trace(String); 가장 상세하게 나온다
log.debug(String); 디버그 상세
log.info(String); 변수의 정보
log.warn(String); 
log.error(String);

log.info("name={}",~); {}를 쓰면 성능 향상화

스프링부트 실행 설정시, 로그 출력 설정을 error로 정하면 그 상위 레벨은 출력이 안된다. (trace,debug,info,warn)
스프링부트 테스트 서버, 개발 서버는 출력 레벨을 자세하게 하려면 trace, 디버그면 충분하면 debug
스프링부트 운영서버는 로그 출력 레벨을 자세하게 하려면 warn, 절약하려면 error로 실행
warn은 용량 초과의 위험

파일은 MultiPartFile 클래스로 다룬다

단위 테스트 : 폴더에 같은 패키지 같은 클래스명으로 같은 메소드로 Test
assertThat 사용

사용 예시
String ext1 = this.fileUtil.getExtention("aaaa.jpg");
assertThat(ext1).isNotNull();
assertThat(ext1).isEqualTo("jpg");

String ext2 = this.fileUtil.getExtention("aaaa.112.jpg");
assertThat(ext2).isNotNull();
assertThat(ext2).isEqualTo("jpg");

String ext3 = this.fileUtil.getExtention("aaaa.jpeg");
assertThat(ext3).isNotNull();
assertThat(ext3).isEqualTo("jpeg");

트랜잭션, 롤백
@Transaction : 스프링 부트에서 트랜잭션 처리를 위한 어노테이션
이 어노테이션으로 선언된 메소드는 exception이 발생하면 롤백을 시킬 수 있다
롤백을 발생시키면 db에 업로드 되지 않게 할 수 있다.
가능한 예외 : RunTimeException(이 예외에서만 롤백 가능), 
불가능한 예외 : Exception(롤백 불가)
Exception을 롤백하고싶으면 메소드에 @Transactional(rollbackFor = {롤백하고싶은클래스.class}) 어노테이션을 붙인다.
롤백 대상을 catch(예외 e) 안에서 다시 던지지 않으면 롤백이 안되기 때문에 throw e를 해야한다.

서버에 파일을 업로드할때 확장명은 올리지 않는다.
왜냐면 보안이 제대로 지켜지지않을때 파일을 다른 누군가가 볼수도 있다.
-----------------------------
04-28
개발할때 프로젝트 순서
1. 개념 모델링, 논리 모델링 , 물리모델링
2. 화면 설계
3. Dto, Entity, 공통 로직(AA)
4. Repoitory(JPA/Mybatis)
5. Service
6. Controller(REST/Web)

JPA에서 Dto, Entity에서 외래키를 참조할때 클래스에 Join클래스의 Dto/Entity와 Join클래스 Id를 넣어놓는데 이건 JPA를 위한것이다.
mybtis는 외래키의 Join클래스의 Id만 있어도 된다.
mybatis를 쓸 경우 조인되는 아이디와 객체의 아이디 값은 항상 같게 해야한다.

JPARepository는 기본적으로 save(),findById(), deleteById(), findAll()를 지원한다.
JPA에서 조회할때는 select쿼리를 메소드이름으로 만들 수 있다.
findBy~, findAllBy, 이런 이름으로 메소드를 만들면 select쿼리 실행이 가능
findBy멤버변수조회조건정렬조건..();

@어노테이션 : 자바에서 컴파일, 빌드시에 도움을 주는 도구

@Transient // JPA에 테이블을 어떤 컬럼에도 연결시키지 않는다.

@RestController json형식으로 데이터를 주고 받는 컨트롤러 클래스로 만들어준다.

@Autowired : 생성자를 따로 적지 않아도 스프링 부트가 인스턴스로 만들어준다, @Service, @Component 어노테이션이 있는 클래스에만 가능하다.

@Mapping : 
URI => http://주소/aaa/bbb/ccc
Method => 해당매핑
요청데이터 => {"id":값, "name":"값2", ...} (Json)

@JoinColumn 외래키임을 표시, @ManyToOne N:1으로 조인 등.. 조인될 Entity객체 위에 적는다. (JPA)
@ManyToOne(fetch = FetchType.LAZY) 일일이 다 select로 찾아준다.
@ManyToOne(fetch = FetchType.EAGER) 조인을 만들어준다.
내가 직접만든 select문은 EAGER를 써도 조인을 해주지 않는다.
그래서 
@EntityGraph를 써서 조인으로 가져와라 라고 내가 만든 select문 함수 위에 적어주면 조인을 다시 해준다. (attributePaths ="{외래키멤버변수이름}" 씀)

controller : url만들고 웹서버를 만들어 준다.(컨트롤러에 해당하는 주소를 입력하면 처리해준다.)
RESTFUL API : http/https로 Json 데이터 통신을 하는 국제 표준방식
restController : json형식으로 데이터를 주고 받는 컨트롤러.
RestponseEntity

-----------------------------------------------------------------------------------
04-29
Spring Boot Web 서버 프로젝트 개발 시작 (SwimPool Project)
1. IntelliJ 에서 New Project > Spring Boot > (JDK버전, Maven/Gradle, groupId, artifactId(프로젝트이름), 버전, dependencies 추가(SpringBoot DevTools, Lombok, Spring Configuration Process, Spring Web, Spring Data JPA, MySQL Driver))
2. Database DDL 언어를 만들어야 한다. create dababase ..., create user ..., create table ...
다만 JPA 는 create table 은 없더라도 자동으로 테이블을 생성할 수 있다.
2-1. dbeaver 를 사용하여 root 로 접속하고 SQL 편집기에서 create database, create user 명령어를 실행해야 한다.
3. application.yml 에 Database 접속정보, charset, 환경설정 정보를 기입한다.
4. XXXApplication.java 파일을 Debug 로 실행해본다.
5.globalExceptionHandler 클래스를 공통 패키지에 만든다. 모든 에러를 Json으로 받기 위해
6.보안을 위해서 기존 Dto, Entity 이외에 RequestDto, ResponseDto를 더 만들어서 관리해야한다.
7.Entity를 이용하여 @Repository Repository인터페이스를 만들어야한다. extends JpaRepository<Entity, Long(Integer)>, nosql db는 String
8.Dto crud, findBy컬럼Containing(검색 조회조건, Pageable), Page<Dto>,Slice<Dto> return Dto 메소드를 @Service 서비스클래스에서 만들어야한다.
9.@RestController 클래스에서 @RequestMapping("사용할 url") 적용,
메소드 위에 @XXXMapping이 존재해야한다. 인자에도 @RequestBody, @RequestParam같은 어노테이션이 있어야 한다.
ResponseEntity<CommonResponse<Dto>> insert,update( Dto dto), deleteById,findById(Long id) return ResponseEntity<CommonResponse<Dto>> 
ResponseEntity<CommonResponse<<Slice<Dto>>> return ResponseEntity<CommonResponse<<Slice<Dto>>> 

로그는 내부 개발자들이 에러를 고치기 위해서 필수로 봐야하는 정보
클래스위에 @SLf4j를사용하면 로그사용 가능, 메소드 내부에서 log.trace(String msg); log.debug(String); log.info(String); log.warn(String); 
log.error(String); 
로그 설정 : 로그레벨설정(위에), 로그출력표현 형태, 로그 파일명, 로그 로테이션(파일을 어떻게 주기적으로 관리할것인가) 로그맥시멈파일사이즈 ... 설정을 스프링부트에서 관리해야한다.

logging:
  level:
    root: info
    com.mjc813.swim.: debug
    org.springframework.web: warn

Entity나 Dto의 @ToString 주로 로그 출력용
형변환용 copyMembers의 forced(doCopy) 는 src에 어떤 값이든 넣으라는 용도를 위한 것

DispatcherServlet : 서버 연결을 준비해주는 서블릿클래스

findAll은 쓰지 않는다.
여러개 찾을때 list 대신 slice, pageable을 쓰는것이 좋다
DBeaver의 커넥션에서 db이름, 유저, 비밀번호로 만드는 것이 좋다.
-----------------------------------------------------------------------------------

05-04
PatchVariable에는 공백이 있으면 안된다.
기본으로 로그 저장은 톰캣로그에 저장, 따로 만들어야 한다.

프로젝트를 인텔리제이 없이 실행
bulid후 jar 파일이 생성, jar 파일을 터미널에서 실행하면 실행 가능하다.(java -jar ~.jar)
취소는 ctrl c 누르기. 로그도 터미널 화면에 나온다.
~.jar > app.log 를 실행하면 app.log 로그가 나오는 파일이 만들어진다. (스프링부트가 아니라 window에서 만드는것)
개발서버/운영서버는 보통 리눅스
(cd  폴더 
 java -server -jar ~.jar &)

aws활용
console.aws.com 이동, sign in using root user email 클릭, 이메일, 암호 입력 후 compute(컴퓨팅) > ec2로 이동
EC2에 대한 서비스(대쉬보드)가 보임, 

Instances(running) : ec2 서비스 현재 사용하는 수
Elastic IPs : 인터넷 공인 ip 사용중인 수
Key Pairs : EC2들에게 접속 할 수 있는 개인 키의 개수
Placement Groups : 그룹 수
Snapshots : EC2 등인 클라우드 시스템의 현재 상태를 그대로 스냅샷을 찍어서 저장(백업) // (유료 조심) 스냅샷을 찍은 상태일때 돈이 나감 
Auto Scaling : 자원(CPU,SSD) 쓰는 양을 늘려줌 up, down 있다.
Instances : ec2 서비스 사용하는 수
Load balancers : 분산환경 구축 용도 수
Security groups : 보안?
Volumes : 저장공간 (사용 거의 안하고 아마존 s3 서비스에서 저장한다.)

서비스를 생성할때 항상 regions(지역)를 확인 // 아시아, 유럽, 북미 등등  
이 지역에 만든 서버는 다른 지역으로 이동이 불가능.

Launch an instance
이름입력, cpu, ram, sdd, os, 그 외 기타 서비스 옵션, 무료만 사용할 것.

Key Pair 생성 시 잘 보관, putty-gen으로 만들어서 올릴 수 있다.
생성할 때 (.pem)openSSH로 맥북, 리눅스용도 / (.ppk) PuTTy로 윈도우 용도.
만들어진 파일 잘 보관.

network는 기본 세팅을 사용, security group(보안 그룹)도 기존 것을 사용, 없으면 새로 만들어서 사용
Allow ssh trafic from 0.0.0.0/0 (모든 ip 접속 가능) , 자기의 공인 ip을 안다면 공인 ip를 입력 가능, 그 공인 ip대역에서만 접속 가능.
보안그룹을 만들었으면 인바운드 규칙 변경해서 22번, 로컬포트 추가할것
이것을 해야 putty에서 접속 가능.

configure storage(스토리지 구성)
8기가 gp3
file System(파일시스템) 
EFS

VPC가 없으면 새로 만든다. 기존 것이 있다면 써야 서로 통신이 가능
10.0.0.0/28
10.0.0.0~10.0.0.15번 ip까지 나의 네트워크 호스트들이 ip를 사용 할 수 있다.
VPC안에 subnet이 있다면 기존의 것을 사용, 없다면 새로 만들기.

instances에서 관리, connect terminate instance로 완전히 지워야 요금이 안나감

ppk파일 고장

PuTTy로 서버관리
session에서 ip adress에 퍼블릭 IPv4 주소(3.34.124.234 등록
putty.exe 실행, connection > ssh . auth > credentials~ 가서 내 ppk등록 
session에서 save하면 자동으로 불러와짐 그걸 실행, 항상 실행으로 하면 PuTTy에서 관리할 준비 끝.
cd .ssh /  authorized_keys 파일로 키 관리 (추가등록 및 삭제)

파일질라로 서버 관리
가장 왼쪽위 버튼(사이트 관리자) 누르고 새 사이트 누르기
프로토콜 SFTP로 바꿈
호스트에 퍼블릭 IPv4 주소 등록
로그온 유형 키파일
사용자 ubuntu
키 파일에 내 ppk파일 넣기

---------------------------------------------------------
05-06
- Key Pair 가 고장 났을 경우에 새로 만드는 방법
PuttyGen 을 실행하세요.
Generate 클릭 하세요
마우스를 흔드세요
개인키가 생성됩니다. 이것을 *.ppk 로 저장하세요.
puttgen 화면의 공용키는 서버의 .ssh/authorized_keys 파일에 내용을 추가하세요.

- Ubuntu 26.04 에서 JRE 21 설치
sudo apt update
sudo apt upgrade
sudo apt install openjdk-21-jre-headless -y
java --version

- Ubuntu 26.04 에서 docker 설치
# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

독커 설치
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo systemctl status docker
sudo systemctl start docker

자바 실행 프로그램과 독커 준비 끝, 실행 할 자바 프로그램과 docker-compose.yml 파일이 필요
docker 폴더 밑 mysql(버전) 폴더 만들고 그 밑에 docker-compose.yml 파일 준비해서 만들기

sudo docker compose up -d
sudo apt install mysql-client-core

------------------------------------
05-08
yml 파일 local(내 컴퓨터로 실행), dev(개발 서버로 실행), prod(운영서버에서 환경 변수에 의해 실행) 모드를 여러개 나눌 수 있다. (application-local.yml, application-dev.yml, application-prod.yml)
prod처럼 환경변수로 하지 않고 직접 입력하면 깃에 올라갈때 보안상의 위험이 있기 때문에 환경변수를 씀. git에 올리지 않게 한다.

port 80,443 기본

메모리가 부족해서 db서버와 백엔드 서버를 나누어서 관리해야할 수도 있음. (A서버에 jre, B서버에 docker와 mysql 따로 설치)

백엔드 서버에서 db서버로 이동하는법
db서버의 ppk를 pem으로 변경
백엔드서버에 .ssh폴더로 이동하여 id_rsa이름으로 복붙, chmod 400으로 변경
db서버의 프라이빗ip로 접속(ssh {ip})
이러면 백엔드서버에서 db서버로 접속가능
---------------------------------------
05-11
vpc 생성 
IPv4 CIDR 에서 10.0.0.0/24 : 10.0.0.0 ~ 10.0.0.255 까지의 ip만 허용
서브넷 생성
가용 영역 : 내 리전
IPv4 VPC CIDR 블록 : 10.0.0.0/24
IPv4 서브넷 CIDR 블록 : 10.0.0.0/24

이제 인스턴스에 연결을 눌러도 안된다. 인터넷 게이트 웨이와 0.0.0.0 라우팅테이블을 추가해야한다.
vpc  > 인터넷 게이트웨이 > 인터넷게이트웨이 생성에서 간단하게 생성, 오른쪽 위 작업에서 vpc에 연결로 내가 만든 vpc에 이 인터넷 게이트웨이를 추가한다.

라우트 테이블 > 라우팅 편집에 가서
라우팅 추가
0.0.0.0/0 인터넷 게이트웨이
내가 만든 인터넷 게이트웨이로 추가하면 된다.

같은 네트워크의 다른 호스트에 접속하려면 백엔드 서버의 보안그룹의 default를 추가 

엘라스틱 ip 만들어서 dp서버에 연결하고 이후 docker 설치 명령어와 mysql 설치 명령어를 입력하면 실행된다.
할당 해제할 땐 인스턴스 > db서버 엘라스틱 ip 링크 클릭 . 오른쪽 위 작업에서 엘라스틱 ip 해제
후 삭제하면 된다.

DB-server에 도커를 설치한다. 
DB-server에 /home/ubuntu/docker/mysql8 디렉토리를 만든다.
만들고 docker-compose.yml 파일을 db서버에 추가하고 실행하면 이미지가 만들어짐,
mysql 설치 ( sudo apt install mysql-client-core )
mysql -h 127.0.0.1 -u root -p
[mysql> ] 하고 mysql 명령을 받아들일 준비상태가 됨 (나갈땐 exit)

CREATE DATABASE [내db이름] DEFAULT CHARACTER SET utf8mb4;
CREATE USER '[유저이름]'@'%' IDENTIFIED BY [db비밀번호]';
GRANT ALL PRIVILEGES ON [내db이름].* TO '[유저이름]'@'%';
FLUSH PRIVILEGES;

use [내db이름];

CREATE TABLE `[테이블이름]` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `addr1` varchar(200) DEFAULT NULL, (예시)
  `addr2` varchar(200) DEFAULT NULL,
  `lanes` varchar(200) DEFAULT NULL,
  `name` varchar(20) NOT NULL,
  `size` varchar(50) DEFAULT NULL,
  `tel` varchar(30) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=9 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

이렇게 테이블 만들고

sudo java -server -jar -Dspring.profiles.active=prod -DDB_IP=[db 프라이빗 ip] -DDB_USER=[유저이름] -DDB_PASSWORD=[db비밀번호] [내가 실행 할 jar파일] & 

이렇게 스프링부트를 실행 할 수 있다.

mysql -h 127.0.0.1 -u root -p

-------------------------------------------------
2026-05-13
nginx:alpine 가장 가볍고 실행하기 좋은 독커이미지
cmd에서 docker image pull ngnix:alpine 으로 다운 
다운 후 독커 데스크탑에 가면 images에 nginx를 실행할 때 host port에 번호할당 (할당중인 포트랑 겹치지 않게)
docker run -d -p 할당포트:80 --name 컨테이너이름 nginx:alpine (데스크탑에서 그냥 실행 가능)
docker exec -it 컨테이너이름 /bin/쉘(ash) 

확장명 없는 Dockerfile로 간단하게 내 이미지를 만들수 있다.
# docker image file을 만드는 형식 => Dockerfile 파일명
# docker image는 레이어 단위로 구성
# 가장 중요하고 가장 먼저 선언할 것 : 어떤 이미지를 가지고 시작할 것인지 결정
FROM nginx:alpine

# 이미지 만들 때 파일을 복사해라 (이미지운영체제디렉토리 )

COPY ./webpage /usr/share/nginx/html 

webpage 폴더의 리눅스터미널에서 docker build -t 컨테이너이름(name):버전(tag)  .하면 내 image가 만들어 진다.
docker run -d -p 할당포트:80 컨테이너이름 이미지이름(webpage:0.0.1)

nginx 서버 포트적용
3개월 마다 한번 갱신받아서 사용

독커파일만 갱신하면 image를 다시 만들 필요가 없다. (빌드만)
.env 환경변수 파일을 사용
#간단한 예시, .env설정 파일과 함께 사용
FROM eclipse-temurin:21-jre-alpine

WORKDIR /원하는 경로

COPY ./swimPool /원하는 경로

#docker-compose.yml 파일과 함께 사용
ENTRYPOINT ["java", "-server", "-jar" \
	, "-DDB_IP=swimpool_mysql" \
	, "-DDB_USER=swim_user" \
	, "-DDB_PASSWORD=qwer1234!" \
	, "-Dspring.profiles.active=prod" \
	, "/원하는 경로/swim-0.0.1-SNAPSHOT.jar"]

EXPOSE 포트번호

-----------------------------------------------------
05-15

.env 파일은 프로젝트 루트 바로 밑, .env 플러그인 적용
docker-build에서 .env파일 , docker-compose 파일, Dorkerfile로 docker에서 백엔드와 db 서버 따로 나누어서 만들 수 있음

logback-spring.xml 파일로 로그 설정 가능(자세한것은 java과목 파일)

docker-compse.prod.yml이 실행이 안되던 이유?
1. .env 플러그인 없음
2. docker-compose.prod.yml 파일에 
environment :
	MYSQL_ROOT_PASSWORD에서 비밀번호가 틀림. (기존에 1234 사용)?
3.  docker run -d -p 80:80 컨테이너이름 이미지이름으로 실행 << 이게 제일 큰 문제였을듯
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d 이렇게 실행 했어야함.

이렇게 하면 db 정보가 없는 문제가 있음
init.sql 파일을 같은 위치에 만들고 db, 유저, 패스워드 만드는 그 문장을 넣고
docker-compse.prod.yml 파일의 volumes 밑에
  volumes:
    - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro

이렇게 추가해줘야함.

기존것 down시키고 up하면 계속 insert 문장실행하는 문제 (스프링부트가 계속 재 시작)

springboot 4 부터는 schema.sql, data.sql 을 사용하면 안되고, flyway 라는 라이브러리를 사용
flyway_schema_history 테이블이 자동으로 생성

